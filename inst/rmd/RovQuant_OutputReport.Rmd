---
params:
  species: "bear"
  years: 2020:2023
  working_dir: "C:/Users/pidu/AQEG Dropbox/AQEG Team Folder/RovQuant/bear/2023/RovQuant_test/test2"
title: "`r paste(params$species, params$years[1], 'to', params$years[length(params$years)])`"
subtitle : "RovQuant - OPSCR Results"
output:
  html_document: 
    theme: "cerulean" 
    toc_depth: 2
    toc: yes
    toc_float: yes
    df_print: kable
    #css: bootstrap.css
    highlight: zenburn
  pdf_document: default
  word_document: default
---


```{r, enviro, echo = FALSE, warning = FALSE, message = FALSE}
years <- if(is.null(params$years)){ 
  2012:as.numeric(format(Sys.Date(), "%Y"))
} else { 
  params$years
}
n.years <- length(years)
species <- params$species
working_dir <- params$working_dir

if(sum(grep("bear", species, ignore.case = T))>0|sum(grep("bjorn", species, ignore.case = T))>0){
  engSpecies <- "bear"
  norSpecies <- "Bjørn"
}
if(sum(grep("wolf", species, ignore.case = T))>0|sum(grep("ulv", species, ignore.case = T))>0){
  engSpecies <- "wolf"
  norSpecies <- "Ulv"
}
if(sum(grep("wolverine", species, ignore.case = T))>0|sum(grep("jerv", species, ignore.case = T))>0){
  engSpecies <- "wolverine"
  norSpecies <- "Jerv"
}

months = c("January","February","March","April","May","June",
           "July","August","September","October","November","December")


##-- load libraries
library(kableExtra)
library(ggplot2)
library(sf)
library(ggmap)
library(patchwork)
library(mapview)
mapviewOptions(fgb = FALSE)
library(rovquantR)
#library(magick)

##-- Load pre-processed habitat shapefiles
data(COUNTRIESWaterHumans, envir = environment()) 

COUNTRIES <- COUNTRIESWaterHumans[COUNTRIESWaterHumans$ISO %in% c("SWE","NOR"), ] %>%
  dplyr::group_by(ISO) %>%
  dplyr::summarise()
```

### `r Sys.Date()`

# Introduction

This document summarizes the output processing of the [RovQuant](https://www.nmbu.no/en/projects/rovquant) analysis of the **`r species`** DNA data available from [RovBase 3.0](https://rovbase.no/).



------------------------------------------------------------------------

# Rovbase data

```{r, data, echo = FALSE}
load(file.path(working_dir, "data/Data.RData"))
```



## Population abundance

The main results of interest is population size:


\includepdf[pages={-}]{my_pdf.pdf}

```{r, abundance, include=TRUE, fig.align="center", fig.cap=c("Figure 1: Total (black) and sex-specific (blue: males, red: females) annual bear population size estimates"), echo = FALSE}
knitr::include_graphics(file.path(working_dir, "figures/Abundance_TimeSeries.png"))
```

```{r }
# panel.upr <-  image_read_pdf(file.path(working_dir, "figures/Abundance_TimeSeries.pdf"))
# plot(panel.upr)
```



## Population density

```{r, density, fig.align="center", fig.cap=c("Figure 1: Brown bear density in Norway in 2023. Density was estimated with an open-population spatial-capture recapture model fitted to NGS and dead recovery data collected between 2020 and 2023"), echo = FALSE}
knitr::include_graphics(file.path(working_dir, "figures/UD_Density_LastYear.png"))
```


------------------------------------------------------------------------

# Metadata

```{r, metadata, echo = F}
utils::sessionInfo()
```
